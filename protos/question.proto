syntax = "proto3";

service QuestionService {
  rpc QuestionCall(QuestionRequest) returns (QuestionResponse);
}

message QuestionRequest {}

message QuestionResponse {
  repeated string question = 1;
}
